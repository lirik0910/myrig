const styles = theme => ({
	formControl: {
		margin: theme.spacing.unit,
		width: '100%',
		overflowY: 'scroll'
	},
	selectEmpty: {
		marginTop: theme.spacing.unit * 2,
	},
});

export default styles;